{
  "installCommand": "npm ci --prefix ../..",
  "buildCommand": "npm run build --workspace=@ai-quiz-coach/shared --prefix ../.. && npm run build --workspace=@ai-quiz-coach/web --prefix ../..",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
