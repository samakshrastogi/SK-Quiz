{
  "installCommand": "npm ci",
  "buildCommand": "npm run build --workspace=@ai-quiz-coach/shared && npm run build --workspace=@ai-quiz-coach/web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
